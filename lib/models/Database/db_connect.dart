import 'package:demo_app/models/Questions/qustions_models.dart';
import 'package:http/http.dart' as http; // the http package

class DbConnect {
  // lest first create a function to add a Question to our database

  Future<void> addQuestion(Question question) async {}
}
